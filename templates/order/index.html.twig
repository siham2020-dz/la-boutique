{% extends 'base.html.twig' %}


{% block body%}
	<div class="container my-5 small-container">

		{{ form_start(deliverForm) }}

		{{ form_row(deliverForm.adresses) }}
		<a href="{{path('app_account_address_form') }}" class="small">Ajouter une nouvelle addresse de livraison
		</a>
		<hr>
		{{ form_row(deliverForm.carriers) }}
		{{ form_end(deliverForm) }}

	</div>
{% endblock %}
