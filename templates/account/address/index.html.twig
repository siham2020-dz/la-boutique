{% extends 'base.html.twig' %}



{% block body %}
<div class="container my-5">
<h1>Espace membre!</h1>


<div class="row">
  <div class="col-md-3">
 
      {% include "account/menu.html.twig" %} 

  </div>
  <div class="col-md-9">
  <div class="d-flex justify-content-between">
    <h5>Mes adresses </h5>
    <a href="{{ path('app_account_address_form') }}" class="btn btn-light">Ajouter une adresses </a>
  </div>
  {%if app.user.adresses|length == 0%}
  <p> Vous n'avez pas encore enregistré d'dresse sur la boutique française</p>
  {% else %}

    <div class="row">
    
     {%for addresses in app.user.adresses %}
     <div class="col-md-4 mt-2"> 
     <div class="bg-light p-2 text-center">
     <strong>{{ addresses.firstname }}  {{ addresses.lastname }}</strong></br>
     {{ addresses.address }}</br>
     {{ addresses.postal }}, {{ addresses.city }}</br>
     {{ addresses.country }}</br>
       <a href="{{ path('app_account_address_form',{ id : addresses.id })}}" class="small text-secondary">Editer</a>
     <a href="{{ path('app_account_address_delete',{ id : addresses.id })}}" class="small text-secondary">Supprimer</a>
     </div>
     </div>
   

     {% endfor %}
    </div>
  {% endif %}
  
  </div>
</div>



</div>
{% endblock %}